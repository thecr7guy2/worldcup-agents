# Copy to .env and fill in. .env is gitignored — never commit real keys.

# ---- Data sources ----
# API-Football (api-sports.io) — fixtures, results, stats, injuries, lineups
API_FOOTBALL_KEY=
# The Odds API (the-odds-api.com) — 1X2 match-winner odds
ODDS_API_KEY=

# ---- LLMs (all 5 models + the intelligence agent routed through OpenRouter — one key) ----
# Requires a funded OpenRouter balance: the intelligence agent uses Opus 4.8 + web search.
OPENROUTER_API_KEY=
